import { BeforeCall, AfterCall, AfterError } from "./interfaces";

export default function (config: any) {
    const {} = config;

    return ({

        // $before_call ({ path, args, iid, src }: BeforeCall) {

        // },

        // $after_call ({ path, args, iid, src, result }: AfterCall) {
        
        // },

        // $after_error ({ path, args, iid, src, error }: AfterError) {

        // }

    })
}